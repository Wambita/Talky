name: my_chat_app
description: chat app using Supabase and Flutter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^1.1.0
  flutter_bloc: ^8.0.1
  timeago: ^3.1.0
  flutter_native_splash: ^2.0.1+1
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  build_runner: ^2.3.3


flutter_native_splash:
color: "#b01c2e"
background_image: "assets/logo.png"

  
 

flutter:
  assets:     
    - assets/images/
  uses-material-design: true
